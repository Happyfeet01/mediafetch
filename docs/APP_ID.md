# MediaFetch app ID

The permanent public Nextcloud app ID is:

```text
mediafetch
```

Release packages must contain the application in a top-level directory named `mediafetch`, matching `<id>mediafetch</id>` in `appinfo/info.xml`.
